# protobuf 文件存储

此路径下存储着解析部分数据所需要的 Protocol Buffers 文件。

路径内 protobuf 文件的根路径为本目录。

## 编写指引

在此路径下所有 protobuf 文件的根路径为本目录。

新增文件时，以此路径为根目录，按照包名依次建立文件夹和文件。

文件的缩进统一使用制表符（ Tab ）来缩进。

若 protobuf 文件的某个依赖项在此路径下有记录，优先使用已有的文件。

在每个 消息 / 字段 的上一行用注释简要描述这个 消息 / 字段 的用途。

有特殊需要时可在文件头部使用注释来提供更多信息。
